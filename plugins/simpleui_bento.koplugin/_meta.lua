return {
    name        = "simpleui_bento",
    fullname    = "Simple UI Bento Grid",
    description = [[Transforms the Simple UI homescreen from full-width rows into a
flexible bento grid. Each module can be configured to occupy 20–100% of the row
width so that multiple modules sit side-by-side on the same row.]],
    version     = "1.0.0",
    author      = "Community",
}
